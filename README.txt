if you want to download the source file and build it yourself
Put the .py files in the same folder and do not forget to build with cmd, of course it is important to install the requirements first

pip install PyQt6 psutil cx_Freeze
python setup.py build
